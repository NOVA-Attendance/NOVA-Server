# Use Python 3.8–3.11 on Windows (TensorFlow has no wheels for Python 3.14).
# Create venv: py -3.8 -m venv .venv  then  .venv\Scripts\activate  then  pip install -r requirements.txt

flask>=2.2
flask-cors>=3.0
flask-sqlalchemy>=3.0
python-dotenv>=1.0
sqlalchemy>=2.0
deepface>=0.0.90
tensorflow>=2.12,<2.16
Pillow>=9.0
